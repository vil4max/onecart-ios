# OneCart Family 1.6.0

## Candidate

- Version/build: **1.6.0 (1)** locally for the app and widget extension, Debug and Release;
  iPhone only. Xcode Cloud assigns the uploaded build number.
- Type: feature release (MINOR) after [1.5.1](1.5.1.md). Task brief:
  [redesign-ios27.md](../../tasks/redesign-ios27.md).
- Scope:
  - Redesign of every screen with iOS 26/27 system idioms. The information architecture is
    unchanged: three tabs (Cart, History, Settings) and the core path of
    [product.md](../../requirements/product.md).
    - Cart: native inset-grouped list with swipe actions, trip progress at the top of the
      cart, a glass add button that morphs into the name-only composer.
    - History: days as a native list; a day opens a read-only detail sectioned by category.
    - Settings: a native `Form` in three groups (cart, look & feel, account); a theme sets
      the app icon and the accent color; appearance and language follow the device, with one row to
      the app's page in the system Settings; members with swipe to remove.
    - Welcome: brand hero, feature rows and Sign in with Apple built from system components.
    - Widget: rows, progress line and empty state share the cart's visual vocabulary.
  - Architecture: `AppSession` and its services are `@Observable`; each screen has a
    ViewModel that depends on service protocols; screens no longer reach `AppSession`
    ([architecture.md](../../engineering/architecture.md)).
  - Member names: each member's chosen name is shared with the cart through a synced member
    profile (Core Data V8, CloudKit record type `CD_MemberProfile`), so the members list,
    widget and join notifications show names instead of "Member"; when Apple provides no
    name, the app asks once. Members on older versions appear as "Member N" until they
    update.
  - Fix: category refinement no longer undoes a rename made while it runs (REQ-CART-020).
  - Fix: Delete Account is announced as a button by VoiceOver.
  - Tests: ViewModel, service and hosted-view tests; all 49 requirements are traced to tests.
- Behaviour deltas, all within the existing requirements:
  - Renaming an item uses a system alert instead of inline editing.
  - The members button is always in the cart toolbar.
  - The busy overlay appears only after 400 ms.
  - Destructive actions in Settings are confirmation dialogs.
  - The owner's sharing warning moved to the footer of the cart section.
  - Theme, accent and language pickers are gone; a stored theme, accent or in-app language
    from an older version is dropped at launch (the per-app language set in the system
    Settings is kept).
- Release prerequisite: `CD_MemberProfile` deployed to the CloudKit Production schema before
  any Release build of this version reaches testers or App Review.
- App Store: 6.9-inch screenshots were refreshed for the redesign
  ([assets/store/README.md](../../../assets/store/README.md)).

## What’s New — English

- A fresh look built for iOS 27: the cart, History, Settings and the widget now use native
  lists, glass controls and the new tab bar.
- Family members now see each other's names. If your Apple ID has no name, the app asks what
  your family should call you.
- Trip progress sits at the top of the cart, and the add button turns into the input field in
  place.
- Simpler Settings: a theme sets the app icon and its color; language and appearance follow
  your iPhone.
- History opens each day as a read-only list grouped by category.
- Renaming an item no longer gets undone when its category is updated.

## What’s New — Russian

- Новый дизайн для iOS 27: корзина, история, настройки и виджет используют системные списки,
  стеклянные элементы и новую панель вкладок.
- Участники семьи теперь видят имена друг друга. Если в Apple ID нет имени, приложение
  спросит, как вас называть.
- Прогресс покупок — вверху корзины, а кнопка добавления превращается в поле ввода прямо на
  месте.
- Настройки проще: тема меняет иконку и цвет приложения; язык и оформление следуют
  настройкам iPhone.
- В истории каждый день открывается списком по категориям, только для просмотра.
- Переименование товара больше не отменяется, когда уточняется его категория.

## Status

- Round 1: `tf-1.6.0-1` on `3c2b2d5` after `just tf-check` printed Ready (GitHub Actions
  `Tests` green on that commit) → build **116**, uploaded 12:09 PM, September 22, 2026, Ready to
  Submit for Friends and Family; What to Test from the tag annotation pasted into the build.
- The first push (`ff86d5d`) failed `Tests` on a SwiftFormat indent rule that the local and CI
  SwiftFormat releases apply differently; fixed forward in `3c2b2d5`.
- Round 2: `tf-1.6.0-2` on `a12ee19` → build **117**, after the `CD_MemberProfile` schema deploy
  to CloudKit Production. The owner checked it on device and asked for the icon-and-color row to
  be called a theme.
- Round 3: `tf-1.6.0-3` on `c8390cf` (Theme wording only) → build **118**, the build submitted
  to App Review (owner confirmation, 2026-09-22).
- App Store: the owner took over the App Store Connect steps (build selection, screenshots,
  What's New wording, submission) and reported them done on 2026-09-22. An App Store Connect
  screenshot from the owner on 2026-09-22 shows 1.6.0 **Waiting for Review**; the owner confirmed
  the same day that build 118 (`c8390cf`) is the one in review. The `v1.6.0` tag belongs on
  `c8390cf` and is the owner's to create.
