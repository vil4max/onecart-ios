# OneCart Family 1.2.1

## Candidate

- Version/build: **1.2.1 (1)** locally for the app and widget extension, Debug and Release; iPhone only.
  Xcode Cloud assigns the uploaded build number.
- Previous App Store version: **1.2 (91)**, Ready for Distribution on September 16, 2026.
- Type: fix-only (PATCH) — no new user-facing features since 1.2.
- Scope:
  - Widgets: the family switch handles the iOS 27 `systemExtraLargePortrait` case, removing the
    archive warning (`4c956c1`); explicit white light-mode widget background (`e23119a`).
  - CI: tests run in GitHub Actions; Xcode Cloud archives only from the CI-moved `testflight`
    and `release` branches ([ADR 0003](../../decisions/0003-ci-split.md)). First release shipped
    through a `v1.2.1` tag.
  - Docs: spec pyramid and approved core, CI branch rules, release runbook.
- Verification on September 17, 2026: `just verify` passed locally (200 XCTest, 0 failures);
  GitHub Actions `Tests` green on `main`; Xcode Cloud "Internal TestFlight (verified main)"
  build 101 (widget fix) archived with 0 warnings.
- Not verified in this release: signed two-device CloudKit sharing and widget interaction on
  iOS 27 devices.

## What’s New — English

- Home Screen and Lock Screen widgets are ready for iOS 27.
- Stability improvements.

## What’s New — Russian

- Виджеты на экране «Домой» и экране блокировки готовы к iOS 27.
- Улучшена стабильность.

## Status

Filled in after App Store Connect submission; see the end of this file.
